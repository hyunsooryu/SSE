const eventSource1 = new EventSource('http://localhost:8080/sub');

eventSource1.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}

const eventSource2 = new EventSource('http://localhost:8080/sub');

eventSource2.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}

const eventSource3 = new EventSource('http://localhost:8080/sub');

eventSource3.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}

const eventSource4 = new EventSource('http://localhost:8080/sub');

eventSource4.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}

const eventSource5 = new EventSource('http://localhost:8080/sub');

eventSource5.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}

const eventSource6 = new EventSource('http://localhost:8080/sub');

eventSource6.onopen = function(event){
    console.log("Connection opened to server-sent events stream")
}




